## 📘 Teacher ↔ Timetable Relationship Documentation

### `TeacherInfo`
Информация о преподавателе. Может существовать **без расписания**, но расписание не может существовать без учителя. Можно удалить расписание у преподавателя и вместо ссылки вставится `NULL`.

### `TeacherTimetable`
Содержит расписание конкретного преподавателя. Жёстко связано с `TeacherInfo` (один к одному).

## 📘 Group ↔ Student_info ↔ Student_timetable Relationship Documentation
### `groups` + `student_info` + `student_timetable`
| Таблица               | Связь                         | Описание                                   |
| --------------------- | ----------------------------- | ------------------------------------------ |
| **groups**            | `1 → N` со `student_info`     | Одна группа имеет много студентов          |
| **groups**            | `1 → 1` с `student_timetable` | У каждой группы одно расписание            |
| **student_info**      | `N → 1` с `groups`            | Каждый студент принадлежит одной группе    |
| **student_timetable** | `1 → 1` с `groups`            | Каждое расписание относится к одной группе |
