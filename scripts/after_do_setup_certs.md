## Шаг 1: Установка `mkcert`

### 🐧 Установка на Linux

Используйте `apt`:

```bash
sudo apt install mkcert
```

### 🍎 Установка на macOS

Используйте `brew`:

```bash
brew install mkcert
```

### 🪟 Установка на Windows

Используйте `choco`:

```bash
choco install mkcert
```

## Шаг 2: Выполнение Скрипта `setup_certs.sh` !!!Выполнять из католога VSUET!!!

После установки `mkcert` выполните скрипт `setup_certs.sh` в терминале. Этот скрипт сгенерирует необходимые файлы сертификатов (`localhost.pem` и `localhost-key.pem`) и поместит их в нужную директорию (`./certs/`), которая используется вашим контейнером Nginx.

```bash
./scripts/setup_certs.sh
```